# ITAMHack - Платформа для хакатонов

Веб-платформа для поиска сокомандников и формирования команд на хакатонах.

## 🚀 Быстрый старт

### Разработка

```bash
# Клонировать репозиторий
git clone <repository-url>
cd ITAMHack

# Настроить переменные окружения
cp env.example .env
# Отредактируйте .env файл

# Запустить в режиме разработки
docker-compose up
```

### Продакшен

См. подробную инструкцию в [DEPLOY.md](./DEPLOY.md)

```bash
docker-compose -f docker-compose.prod.yaml up -d
```

## 📁 Структура проекта

```
ITAMHack/
├── backend/          # FastAPI бэкенд
│   ├── api/         # API приложение
│   └── Dockerfile   # Docker образ для бэкенда
├── frontend/         # React фронтенд
│   ├── src/         # Исходный код
│   └── Dockerfile   # Docker образ для фронтенда
├── docker-compose.yaml          # Конфигурация для разработки
├── docker-compose.prod.yaml     # Конфигурация для продакшена
└── env.example                  # Пример переменных окружения
```

## 🛠 Технологии

- **Backend**: FastAPI, SQLAlchemy, PostgreSQL, Redis
- **Frontend**: React, TypeScript, Vite
- **Bot**: aiogram (Telegram)
- **Deployment**: Docker, Docker Compose, Nginx

## 📝 API Документация

После запуска приложения доступна по адресу:
- Разработка: http://localhost:8000/docs
- Продакшен: http://your-domain/docs

## 🔧 Переменные окружения

См. `env.example` для списка всех переменных окружения.

Основные:
- `DB_USER`, `DB_PASSWORD`, `DB_NAME` - настройки PostgreSQL
- `SECRET_KEY` - секретный ключ для JWT
- `TELEGRAM_BOT_TOKEN` - токен Telegram бота
- `REDIS_HOST`, `REDIS_PORT`, `REDIS_PASSWORD` - настройки Redis

## 📚 Документация

- [Инструкция по деплою](./DEPLOY.md)
- [API документация](./backend/api/README.md) (если есть)

## 🤝 Вклад в проект

1. Fork проекта
2. Создайте feature branch (`git checkout -b feature/AmazingFeature`)
3. Commit изменения (`git commit -m 'Add some AmazingFeature'`)
4. Push в branch (`git push origin feature/AmazingFeature`)
5. Откройте Pull Request

## 📄 Лицензия

[Укажите лицензию]

## 👥 Авторы

[Укажите авторов]
